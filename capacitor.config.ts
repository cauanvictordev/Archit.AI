import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cauan.arquitetura',
  appName: 'com.cauan.arquitetura',
  webDir: 'dist'
};

export default config;
